# AuditSuite checkout now mints a unique Razorpay payment link per buyer at request
# time, so the old static NEXT_PUBLIC_AUDITSUITE_PAYMENT_LINK_* values are no longer used.
# Provide Razorpay API credentials instead (Dashboard -> Settings -> API Keys).
RAZORPAY_KEY_ID=
RAZORPAY_KEY_SECRET=
